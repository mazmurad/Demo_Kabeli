
Процедура ПередЗаписью(Отказ)
	
	Если СтрЗаменить(X, " ", "") = "" И СтрЗаменить(Y, " ", "") = "" И СтрЗаменить(Z, " ", "") = "" Тогда
		Возврат;	
	КонецЕсли;
	
	СтрокаКоординат = "" + Формат(X, "ЧГ=0");	
	СтрокаКоординат = СтрокаКоординат + ?(ЗначениеЗаполнено(СтрокаКоординат), "/", "") + Формат(Y, "ЧГ=0");  	
	СтрокаКоординат = СтрокаКоординат + ?(ЗначениеЗаполнено(СтрокаКоординат), "/", "") + Формат(Z, "ЧГ=0");	
	Наименование    = СтрокаКоординат;
	
КонецПроцедуры
